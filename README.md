# This project implements data-points feature in matplotlib

## Problem:

Like in MATLAB we can use data-points to get the data using the mouse. Just like that I was looking for the same feature in the matplotlib which is non-existent. 



## Dependencies

`Matplotlib.pyplot` - This is the only library being used.



